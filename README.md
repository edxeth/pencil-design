# pencil-design

Design and edit UI in Pencil (`.pen`) files via the Pencil MCP server.

## Install

```bash
npx skills add edxeth/pencil-design
```

Works with any agent on [skills.sh](https://skills.sh).
